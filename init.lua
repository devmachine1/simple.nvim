-- main config file for neovim
-- instead of writing everything in one file, kept everything in differnt files so that its more organizied
require("packages")
require("theme")
require("telescope-config")
require("coc-config")
require("config")
